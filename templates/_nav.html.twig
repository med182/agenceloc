
      {% if app.user %}
      {% if is_granted('ROLE_ADMIN') %}
      {% set color = "warning" %}
        {% else %}
        {% set color = "info" %}
      {% endif %}
      {% else %}
        {% set color = "dark" %}
      {% endif %}
      
      <nav class="navbar navbar-expand-lg navbar-dark bg-{{color}}
      ">
        <div class="container-fluid">
          <img class="w-25" src={{ asset("image/rent.png") }} alt="Logo
          symfony">
        </div>
        {# <button
          class="navbar-toggler"
          type="button"
          data-toggle="collapse"
          data-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
     
        </button> #}

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
          <li class="nav-item active">
              <a class="nav-link text-left text-white" href="{{ path('home')}}" >Agence De Location</a>
            </li>
                  <li class="nav-item active">
              <a class="nav-link text-left text-white" href="{{ path('app_user_index')}}" >Gestion des Membres</a>
            </li>
           
                  <li class="nav-item active">
              <a class="nav-link text-left text-white" href="{{ path('app_commandes_index')}}" >Gestion des Commandes</a>
            </li>
           
        
          {% if app.user %}
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false"></a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="{{ path('profil')}}">Profil</a></li>
   {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_admin') }}">Admin</a>
                    </li>
           
                  
             
              {% endif %}   
  

            
            
            <li>
            <a class="dropdown-item" href="{{ path('app_logout')}}">Déconnexion</a></li>
       
        </li>
           </ul>

              {% else %}
          
          
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_register')}}">Inscription</a>
            </li>
         
           <li class="nav-item">
          <a class="nav-link" href="{{ path('app_login')}}">Connexion</a>
            </li>
           
          {% endif %}
             <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_vehicule_index') }}">Vehicule</a>
                    </li>
        </div>
      </nav>
         