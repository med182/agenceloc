{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
  
  
      {{ form_start(registrationForm) }} {# <form> #}

          <div class="form-group">
            <label for="nom">   {{ form_row(registrationForm.email) }}</label>
          </div>

          <div class="form-group">
            <label for="nom">   {{ form_row(registrationForm.nom) }}</label>
          </div>

          <div class="form-group">
            <label for="nom">{{ form_row(registrationForm.plainPassword, {
              label: 'Password'
          }) }}
          
          </div>
          
          <div class="form-group">
            <label for="email"> {{ form_row(registrationForm.prenom) }}</label>
          
          </div>

        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn btn-primary mb-2">Inscription</button>

    {{ form_end(registrationForm) }}{# </form> #}




{% endblock %}
